# Dependabot version updates are DISABLED: Renovate (renovate.json) is the
# single dependency manager for npm, github-actions, docker, docker-compose,
# and helm. Running both produced duplicate grouped PRs.
# Keep Dependabot *security alerts* enabled in repo Settings -> Code security
# and analysis; Renovate consumes them via osvVulnerabilityAlerts.
# Also disable "Dependabot version updates" in that settings page so no new
# Dependabot PRs are opened.
version: 2
updates: []
